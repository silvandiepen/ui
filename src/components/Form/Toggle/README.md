# Toggle

`Toggle` is the standalone alias for the shared `InputToggle` implementation. It supports boolean on/off behavior by default and can also render explicit labeled options with optional icons.
