# InputCascader

Hierarchical selection control for choosing a value across multiple nested levels. It stores the selected path as an array of values and renders each depth as its own selectable column.
