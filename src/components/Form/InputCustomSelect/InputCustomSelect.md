# InputCustomSelect

Custom select input with support for rich option rendering, search filtering, and multi-select.

## Usage

```vue
<template>
  <InputCustomSelect v-model="selected" :options="options" label="Pick items" />
</template>
```
