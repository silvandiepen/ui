# Icon

Shared icon renderer backed by the `open-icon` set.

## Usage

```vue
<Icon name="search" size="large" />
```

## Notes

- Resolves icon names through the shared icon mapping
- Supports size and color variants
- Renders the resolved SVG inline
