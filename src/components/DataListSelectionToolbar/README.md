# DataListSelectionToolbar

Selection-focused toolbar content for `DataList`.

This component is rendered inside the global `Toolbar` shell and receives
selection state/callbacks from the parent `DataList`.
