# Toast

Renders stacked toast notifications via the `toastService`.

## Usage

```typescript
toastService.show({ message: 'Saved!', type: 'success' });
```

Mount `<Toast />` once in your app root to render toasts.

## Props

No props — driven entirely by `toastService`.
