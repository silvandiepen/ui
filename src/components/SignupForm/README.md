# SignupForm

Ready-to-use registration surface with optional name field, provider actions, password confirmation, terms acceptance, marketing opt-in, inline mismatch handling, and footer actions for switching back to sign-in.
