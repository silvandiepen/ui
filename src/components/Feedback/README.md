# Feedback

Legacy feedback namespace that still groups popup, tooltip, and toast surfaces.

## Scope

- `Popup` service-driven modal orchestration
- `Toast` notification helpers and container
- older `ToolTip` exports that overlap with the newer shared tooltip component

## Docs Structure

Use the nested docs pages for the concrete surfaces. The root `Feedback` page is only the namespace overview.
