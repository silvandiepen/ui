# Kbd

Semantic keyboard key surface for shortcut hints, command labels, and compact interaction copy.

## Usage

```vue
<Kbd>⌘</Kbd>
<Kbd>K</Kbd>
```

## Props

- `size`: `small` or `medium`.
- `variant`: `default` or `subtle`.
