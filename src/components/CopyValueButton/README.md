# CopyValueButton

Small reusable icon-only clipboard button that copies a value and raises a success toast containing the copied value.

## Behavior

- Renders as a compact icon-only action for dense surfaces such as cards, badges, and metadata rows.
- Swaps from a clipboard icon to a green checkmark for a short confirmation state after copying.
- Uses the `label` prop for the button tooltip and accessible label.
