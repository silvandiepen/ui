# VideoPlayer

Generic video surface that layers a poster image with a video element and supports eager or hover-based loading, autoplay or hover playback, once or continuous playback, `contain` or `cover` fitting, and optional native controls.
